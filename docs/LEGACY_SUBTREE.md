# Legacy `Wanderlog/` subtree

The production app is built from the **repository root** (`package.json`, `App.tsx`, `vite.config.ts`).

A nested `Wanderlog/` folder was a historical duplicate and has been removed. Do not reintroduce a second app tree; port all changes to the root app.
